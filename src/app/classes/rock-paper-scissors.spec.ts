import { RockPaperScissors } from './rock-paper-scissors';

describe('RockPaperScissors', () => {
  it('should create an instance', () => {
    expect(new RockPaperScissors()).toBeTruthy();
  });
});
